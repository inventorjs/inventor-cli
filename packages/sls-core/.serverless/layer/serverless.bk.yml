app: scf-starter
stage: dev
name: layerdemo
component: layer

inputs:
  name: ${name}-${stage}-${app}
  region: ap-guangzhou
  src:
    bucket: sls-layer-ap-guangzhou-code
    object: sunkeytest-layer-1680230665.zip
  runtimes:
    - Nodejs16.13
